'use strict';

describe('Service: planTrabajoMidService.js', function () {

  // load the service's module
  beforeEach(module('contractualClienteApp'));

  // instantiate service
  var planTrabajoMidService.js;
  beforeEach(inject(function (_planTrabajoMidService.js_) {
    planTrabajoMidService.js = _planTrabajoMidService.js_;
  }));

  it('should do something', function () {
    expect(!!planTrabajoMidService.js).toBe(true);
  });

});
